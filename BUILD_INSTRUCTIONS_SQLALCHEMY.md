# Инструкция по сборке приложения с SQLAlchemy

## ✅ Изменения

Приложение переведено на использование **SQLAlchemy** с **SQLite** вместо MySQL:
- ✅ Убраны зависимости от MySQL и внешних библиотек (libiodbc, OpenSSL)
- ✅ Используется SQLAlchemy 2.0+ (совместима с Python 3.13)
- ✅ База данных SQLite создается автоматически при первом запуске
- ✅ Файл `cars.db` будет создан в той же директории, что и приложение

## 📦 Установка зависимостей

```bash
# Установите все зависимости
pip3 install -r requirements.txt

# Или вручную:
pip3 install PyQt6 SQLAlchemy cx_Freeze
```

## 🔨 Сборка приложения для macOS

### Вариант 1: Использование скрипта (рекомендуется)

```bash
# Сделайте скрипт исполняемым
chmod +x builds/build_cxfreeze.sh

# Запустите сборку
bash builds/build_cxfreeze.sh
```

### Вариант 2: Ручная сборка

```bash
# Очистите предыдущие сборки
rm -rf build dist CarSelection.app

# Установите cx_Freeze (если еще не установлен)
pip3 install cx_Freeze

# Запустите сборку
python3 setup_cxfreeze.py build

# Создайте .app bundle вручную (см. скрипт build_cxfreeze.sh)
```

## 📁 Структура после сборки

```
CarSelection.app/
├── Contents/
│   ├── MacOS/
│   │   └── CarSelection          # Исполняемый файл
│   ├── Resources/
│   │   ├── config.py
│   │   ├── cars.db               # База данных (если была скопирована)
│   │   └── [все зависимости]
│   └── Info.plist
```

## 🚀 Запуск приложения

```bash
# Удалите атрибуты безопасности (для macOS Sequoia)
xattr -cr CarSelection.app

# Запустите приложение
open CarSelection.app

# Или из терминала (для отладки)
./CarSelection.app/Contents/MacOS/CarSelection
```

## 📝 Примечания

1. **База данных**: Если файл `cars.db` не существует, он будет создан автоматически при первом запуске с заполнением данными.

2. **Расположение базы данных**: База данных создается в той же директории, где находится исполняемый файл (для собранного приложения) или скрипт (для разработки).

3. **Совместимость**: 
   - Python 3.13+
   - macOS Sequoia 15.7.3
   - Apple Silicon (arm64) и Intel (x86_64)

4. **Преимущества SQLAlchemy + SQLite**:
   - ✅ Нет внешних зависимостей
   - ✅ Простая сборка
   - ✅ Портативность (один файл базы данных)
   - ✅ Не требует сервера
   - ✅ Быстрая работа для небольших баз данных

## 🔧 Решение проблем

### Ошибка: "SQLAlchemy not found"
```bash
pip3 install SQLAlchemy
```

### Ошибка: "Permission denied" при запуске
```bash
xattr -cr CarSelection.app
chmod +x CarSelection.app/Contents/MacOS/CarSelection
```

### База данных не создается
- Убедитесь, что у приложения есть права на запись в директорию
- Проверьте логи в терминале при запуске

