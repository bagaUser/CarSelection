# Быстрый старт: Альтернативы py2app

## 🎯 Рекомендация для Python 3.13+ на Apple Silicon

### 1️⃣ Briefcase (ЛУЧШИЙ ВЫБОР) ⭐

**Почему:**
- Полная поддержка Python 3.13+
- Отличная работа с Apple Silicon
- Активная разработка и поддержка

**Быстрая установка:**
```bash
pip3 install briefcase
./build_briefcase.sh
```

**Или вручную:**
```bash
briefcase new
# Ответьте на вопросы, затем:
briefcase update
briefcase build macOS
briefcase run macOS
```

Подробнее: см. `BRIEFCASE_INSTRUCTIONS.md`

---

### 2️⃣ Nuitka (ЛУЧШАЯ ПРОИЗВОДИТЕЛЬНОСТЬ) 🚀

**Почему:**
- Компилирует в нативный код
- Отличная производительность
- Меньший размер приложения

**Быстрая установка:**
```bash
pip3 install nuitka
xcode-select --install  # Если еще не установлен
./build_nuitka.sh
```

**Примечание:** Сборка занимает больше времени, но результат быстрее работает.

---

### 3️⃣ cx_Freeze (ПРОСТОТА) 🛠️

**Почему:**
- Простая настройка
- Стабильная работа
- Легко понять

**Быстрая установка:**
```bash
pip3 install cx_Freeze
./build_cxfreeze.sh
```

---

## Сравнение времени сборки

| Инструмент | Первая сборка | Последующие сборки | Размер .app |
|------------|---------------|-------------------|-------------|
| Briefcase  | ~2-3 минуты   | ~1 минута         | ~150-200 MB |
| Nuitka     | ~5-10 минут   | ~5-10 минут       | ~50-100 MB  |
| cx_Freeze  | ~1 минута     | ~30 секунд        | ~200-300 MB |

---

## Какой выбрать?

- **Для разработки и тестирования:** Briefcase
- **Для максимальной производительности:** Nuitka  
- **Для простоты:** cx_Freeze

---

## Общие шаги после сборки

1. **Проверьте архитектуру:**
   ```bash
   file CarSelection.app/Contents/MacOS/CarSelection
   ```
   Должно быть `arm64` для Apple Silicon

2. **Запустите из терминала для диагностики:**
   ```bash
   ./CarSelection.app/Contents/MacOS/CarSelection
   ```

3. **Запустите через Finder:**
   ```bash
   open CarSelection.app
   ```

---

## Устранение проблем

Если что-то не работает:

1. Проверьте версию Python:
   ```bash
   python3 --version
   ```

2. Убедитесь, что зависимости установлены:
   ```bash
   pip3 install mysql-connector-python
   ```

3. Запустите диагностику:
   ```bash
   python3 check_architecture.py
   ```

---

## Дополнительная информация

- Подробное сравнение: `ALTERNATIVES.md`
- Инструкция по Briefcase: `BRIEFCASE_INSTRUCTIONS.md`
- Решение проблем с py2app: `fix_launch_error.md`
